<% if not current_user && current_user.admin? %>
    <div class="class">  
     <strong><%= "You need to pay 1000 deposit amount - Click <a href='#{terms_use_path}'>here</a> for details!".html_safe %></strong>
  </div>
<%end %>
